namespace DevBasics.CarManagement.Dependencies
{
    public enum RegistrationType
    {
        None = 0,
        Register = 10,
        Unregister = 20,
        Reset = 30,
        Override = 40
    }
}
